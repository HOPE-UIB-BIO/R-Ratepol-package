% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_roc.R
\name{plot_roc}
\alias{plot_roc}
\title{Plot the Rate-of-Change sequence}
\usage{
plot_roc(
  data_source,
  age_threshold = NULL,
  roc_threshold = NULL,
  peaks = FALSE,
  trend = NULL,
  silent = FALSE
)
}
\arguments{
\item{data_source}{\code{tibble}. Output of \code{\link[=estimate_roc]{estimate_roc()}} or \code{\link[=detect_peak_points]{detect_peak_points()}}.}

\item{age_threshold}{\code{numeric}. Optional. Upper (oldest) age cut-off; samples older than
this value are excluded from the plot.}

\item{roc_threshold}{\code{numeric}. Optional. Upper RoC cut-off; values above this are clipped.}

\item{peaks}{\code{logical}. If \code{TRUE} and a \code{Peak} column is present in \code{data_source},
peak points are highlighted on the plot (default = \code{FALSE}).}

\item{trend}{\code{character} or \code{NULL}. When \code{peaks = TRUE}, optionally overlay the
trend curve used during peak detection. One of \code{"threshold"},
\code{"trend_linear"}, or \code{"trend_non_linear"}. \code{NULL} (default) shows no
trend line.}

\item{silent}{\code{logical}. If \code{TRUE}, suppress all console output (default = \code{FALSE}).}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot RoC scores through time with an upper uncertainty envelope and,
optionally, a superimposed trend curve and peak-point markers.
}
\examples{
\dontrun{
data("example_data", package = "RRatepol")

sequence_01 <-
  estimate_roc(
    data_source_community = example_data$pollen_data[[1]],
    data_source_age = example_data$sample_age[[1]],
    smooth_method = "shep",
    working_units = "MW",
    rand = 1e3,
    use_parallel = TRUE,
    dissimilarity_coefficient = "chisq"
  )

plot_roc(
  sequence_01,
  age_threshold = 8e3,
  roc_threshold = 1
)
}
}
\seealso{
\code{\link[=estimate_roc]{estimate_roc()}}, \code{\link[=detect_peak_points]{detect_peak_points()}}
}
